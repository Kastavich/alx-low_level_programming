#include <stdio.h>

/**
 * main - prints the sum of all the multiples
 *
 * Return: Always 0
 */

int main(void)
{
	int d;

	int = sum;

	sum = 0;

	for (d = 0 ; d < 10 ; d++)
	{
		if ((d % 3 == 0) || (d % 5 == 0))
		{
			sum = sum + d;
		}
	}
	printf("%d\n", sum);
	return (0);
}
